<?xml version="1.0" encoding="utf-8"?>
<!-- Plain white — the glyph itself carries the Aegean/Rust brand colors
     (ic_launcher_foreground.xml), so a white ground keeps them at full
     saturation instead of competing with a colored backdrop, and matches
     the flat-white-plus-colorful-glyph pattern most launcher icons around
     it use. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M0,0h108v108h-108z"
        android:fillColor="#FFFFFF" />
</vector>
