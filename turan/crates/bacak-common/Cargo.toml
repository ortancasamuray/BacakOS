[package]
name = "bacak-common"
description = "Shared types: config, user enumeration, session discovery, IPC protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
log = { workspace = true }

[dev-dependencies]
# none — keep the core unit-testable with zero system dependencies
